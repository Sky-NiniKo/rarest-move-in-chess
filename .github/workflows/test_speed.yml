name: Test speed
on:
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Test speed
        run: |
          curl -o /dev/null https://database.lichess.org/standard/lichess_db_standard_rated_2016-04.pgn.zst
